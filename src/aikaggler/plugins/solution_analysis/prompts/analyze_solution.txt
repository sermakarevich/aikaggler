You are analyzing a Kaggle competition solution writeup. Extract concrete technical details into JSON following the schema.

Rules:
- Do NOT invent models, papers, URLs, or techniques. If a field is not explicitly stated, leave it empty (empty array or empty string).
- 'summary': 3-5 sentences on what the author actually did, what ideas and insights drove the result. Do NOT copy the title.
- 'extensive_summary': longer-form, several paragraphs of markdown. Cover the key ideas, insights, and technical choices in depth — NOT a blow-by-blow rewrite of the post. Preserve the author's terminology verbatim for model/technique names. Section headers (##) where helpful.
- 'rank': integer position. Typically found in title ('1st Place', '8 place solution', '3rd place') or body. Return null if not derivable.
- 'competition_flow': one sentence summarizing the end-to-end pipeline from raw data to submission.
- 'data_reading': how the data is loaded / formatted / parsed (e.g. 'parquet via pandas', 'webdataset tar shards', 'custom Dataset reading 512x512 RGBY TIFFs').
- 'data_processing': preprocessing, cleaning, normalization, resizing, tokenization, duplicate handling, external data, augmentations (train-time and TTA).
- 'features_engineering': hand-crafted features added on top of raw inputs (aggregations, encodings, lags, interactions, domain-derived, dimensionality reductions, text-from-tabular). Leave empty for pure CV/NLP where no hand-crafted tabular features apply.
- 'models': architecture names mentioned (e.g. 'DenseNet-121', 'LightGBM').
- 'frameworks_used': ML libraries explicitly named (e.g. 'pytorch', 'keras', 'lightgbm', 'transformers', 'fastai').
- 'loss_functions': explicit losses (e.g. 'focal loss', 'BCE with logits', 'ArcFace', 'RMSE').
- 'cv_strategy': validation scheme (folds, holdout, adversarial validation, grouping key).
- 'ensembling': one sentence describing the ensembling + any post-processing (threshold tuning, TTA, calibration).
- 'insights': key takeaways and ideas the author calls out as important — the things a reader should remember. One sentence each.
- 'critical_findings': counterintuitive observations, data quirks, or score-moving discoveries the author explicitly highlights as lessons (distinct from general insights — these are surprising / non-obvious). One sentence each.
- 'what_did_not_work': extract ONLY if the author explicitly lists failed attempts.
- 'github_repos': classify role as 'solution' (author's own code for THIS competition), 'library' (third-party dependency), or 'reference' (mentioned for context, not a dep).
- 'papers': include title and URL (both REQUIRED). Prefer arxiv URLs of form 'https://arxiv.org/abs/XXXX.XXXXX'. Set 'arxiv_id' ONLY if an explicit arxiv number appears. Copy title verbatim — NEVER invent one from the URL alone. If only a URL is present, omit the paper.
- 'other_solution_refs': links to OTHER kaggle kernels/discussions. Only include if an actual URL appears.

Title: {title}

Regex-detected GitHub URLs (use these verbatim, classify role): {regex_urls_json}

Solution markdown:
{body_excerpt}
