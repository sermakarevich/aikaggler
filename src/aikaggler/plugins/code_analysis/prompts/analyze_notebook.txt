You are analyzing a public Kaggle notebook (kernel) shared for a competition. Extract concrete technical details into JSON following the schema. The notebook is typically a tutorial / EDA / baseline / training recipe — not necessarily a winning solution.

Rules:
- Do NOT invent models, libraries, or techniques. If a field is not explicitly stated or visible in code, leave it empty.
- 'summary': 3-6 sentences on what the notebook actually does, what it demonstrates or proves, and the ideas and insights a reader should take away. Do NOT rewrite the notebook. Do NOT copy the title. Focus on the "why", not the "line-by-line".
- 'purpose': 'eda' = exploratory data analysis; 'baseline' = first-pass model; 'feature_engineering' = focused on feature generation; 'training' = full training pipeline; 'inference' = predicting / submission generation; 'ensemble' = combining models; 'tutorial' = explanatory / educational; 'utility' = helper scripts, data download; 'other' otherwise.
- 'competition_flow': one sentence summarizing what the notebook does end-to-end (data → model → output).
- 'data_reading': how the data is loaded / formatted / parsed.
- 'data_processing': preprocessing, cleaning, normalization, resizing, tokenization, augmentations (incl. TTA).
- 'features_engineering': hand-crafted features added on top of raw inputs (aggregations, encodings, lags, interactions, domain-derived, dimensionality reductions). Leave empty for pure CV/NLP notebooks where no hand-crafted tabular features apply.
- 'models': architecture names (e.g. 'ResNet-34', 'LightGBM').
- 'frameworks_used': ML libraries used in code cells (e.g. 'pytorch', 'keras', 'scikit-learn', 'lightgbm', 'transformers', 'fastai').
- 'loss_functions': explicit losses used in training.
- 'cv_strategy': validation scheme shown (e.g. 'StratifiedKFold(n_splits=5)', 'GroupKFold by patient_id', 'holdout 10%'). Empty if not shown.
- 'ensembling': one sentence describing the ensembling + any post-processing. Empty if the notebook trains a single model with no post-processing.
- 'insights': key ideas the author conveys or proves — what a reader would learn from this notebook. One sentence each.
- 'critical_findings': counterintuitive observations the author explicitly calls out (data quirks, label noise, leakage warnings, class imbalance stats). One sentence each. Leave empty if none.
- 'what_did_not_work': extract ONLY if the author explicitly discusses failed attempts / ablations.

Title: {title}
Author: {author}
Keywords: {keywords}

Notebook content (markdown + code cells):
{body_excerpt}
